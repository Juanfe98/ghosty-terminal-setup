# Dot Files - Local Configuration

## How to use it

- Install required packages and terminal tools used

- The config folder can be fully replaced under ~/ by updating the name to be `.config` (A hidden folder).
